# user-secret.yaml
{{ if $.Values.userServer -}}
{{ $context := merge (dict "app" $.Values.userServer) . -}}
{{- include "neves.snipet.secret.header" $context -}}
{{ with .Values.userServer.secret }}
data:
  DB_USER: {{ .db.username }}
  DB_PASSWORD: {{ .db.password }}
{{ end -}}
{{- end -}}
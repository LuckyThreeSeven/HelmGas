# play-secret.yaml
{{ if $.Values.playServer -}}
{{ $context := merge (dict "app" $.Values.playServer) . -}}
{{- include "neves.snipet.secret.header" $context -}}
{{ with .Values.playServer.secret }}
data:
  KEY_PAIR_ID: {{ .s3.keyPairId  | b64enc | quote }}
  CLOUDFRONT_PRIVATE_KEY: {{ .s3.privateKey | b64enc | quote }}
{{ end -}}
{{- end -}}
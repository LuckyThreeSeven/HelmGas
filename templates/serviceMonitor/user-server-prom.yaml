apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-server-prom
  namespace: "{{  }}"
  labels:
    release: "{{  }}"
spec:
  namespaceSelector:
    matchNames:
      - "{{  }}"
  selector:
    matchLabels:
      app: user-server
  endpoints:
    - port: user-basic
      path: "/metrics"
      interval: 15s
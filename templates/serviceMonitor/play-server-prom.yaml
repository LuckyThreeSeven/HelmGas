apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: play-server-prom
  namespace: "{{  }}"
  labels:
    release: "{{  }}"
spec:
  namespaceSelector:
    matchNames:
      - "{{  }}"
  selector:
    matchLabels:
      app: play-server
  endpoints:
    - port: play-basic
      path: "/metrics"
      interval: 15s
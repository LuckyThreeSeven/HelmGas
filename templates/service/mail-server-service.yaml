# mail-server-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: mail-server-service
  namespace: "{{  }}"
spec:
  selector:
    app: mail-server
  ports:
    - protocol: TCP
      name: mail-basic
      port: 80
      targetPort: 8000
  type: ClusterIP
